import random


i=1
repetido = True


while repetido:
    L = random.randint(0, 10)




while repetido > 0:
            repetido = 0
            rank1 = random.randint(0, 15)
            for j in range(n):
                if pais[j].ranking == rank1:
                    repetido += 1
                    break

        rank = rank1



        # for i in range(n):
        for j in range(i):
            if pais[i].ranking == pais[j].ranking:
                j = 0
                pais[i].ranking = random.randint(1, 16)




while norepe:
            # print("ingreso al ciclo")
            if i > 0:
                for j in range(i):
                    if cambio:
                        j = 0
                        cambio = False

                    if pais[i].ranking == pais[j].ranking:
                        print("Numero igual")
                        pais[i].ranking = random.randint(1, 17)
                        print("Numero cambiado, el nuevo numero es: ", pais[i].ranking)
                        cambio = True


                    else:
                        norepe = False
                        print("Numero distinto")

            else:
                norepe = False
                print("Primer numero")






, 'Mexico', 'Grecia', 'España', 'Portugal', 'Australia',
                  'Iran', 'Corea', 'Dinamarca', 'Turquia', 'Polonia', 'Suecia', 'Sudafica',
                  'Croacia', 'Serbia', 'Rusia', 'Islandia', 'Ghana', 'Nigeria', 'Italia'
